<thlogic>
    <attr sel="#headId" th:replace="~{fragments/head::headFragment}"/>
    <attr sel="#play-link" th:href="@{__${T(academy.learnprogramming.util.GameMappings).PLAY}__}" th:text="#{game.play}"/>
    <attr sel="#footerId" th:replace="~{fragments/footer::footerFragment}"/>
    <attr sel="#welcome" th:text="#{game.welcomeMessage}" />
</thlogic>